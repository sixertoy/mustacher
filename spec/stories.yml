src/mustacher.js:
  render:
    - throw if no argument
    - should return a string
    - should return helloworld
  hasOptions:
    - returns false if no arguments
    - returns arguments is empty
    - returns false is not a plainobject
    - returns options property 'name' is not defined
    - expect to returns array of arguments
src/helpers/conditions.js:
  register:
    - handlebars registerHelper called twice
  render:
    - throws missing arguments
    - not throws missing arguments
    - handlebars createFrame called
  or:
    - return true
    - return false
  and:
    - return true
    - return false
src/helpers/equal.js:
  register:
    - handlebars registerHelper called once
  render:
    - throws missing arguments
    - not throws missing arguments
    - handlebars createFrame called
  isequal:
    - return true
    - return false
src/helpers/image.js:
  register:
    - handlebars registerHelper called once
  render:
    - throws missing arguments
    - not throws missing arguments
  returns an image of 300x300:
    - no width no height (300 default)
    - width 300 no height (300 default)
  returns an image of 400x400:
    - width 400 no height (same as width)
  returns an image of 500x220:
    - width 500 height 220
src/helpers/literal.js:
  register:
    - call handlebars registerHelper 3 times
  render:
    - throw no arguments
    - not throw no arguments
    - call mustacher hasoptions once
    - call handlebars createframe once
  delimiters:
    - returns {{
    - returns }}
    - returns exotic user define delim [*
    - returns exotic user define delim *]
src/helpers/livereload.js:
  register:
    - call handlebars registerHelper once
  render:
    - throw no arguments
    - not throw no arguments
    - call mustacher hasoptions once
  render with defaults:
    - returns debug true port 1337
  render with debug setted only:
    - returns debug true port 1337
    - returns empty string debug false port 1337
  render with port setted only:
    - returns debug true port 53739
  render with port setted and debug setted:
    - returns debug true port 53739
    - returns empty string debug false port 53739
src/helpers/random.js:
  register:
    - call handlebars registerHelper once
  render:
    - throw no arguments
    - not throw no arguments
    - call mustacher hasoptions once
  render with arguments:
    - returns a number between 0 and 1
    - returns a number between 0 and 2
    - returns a number between 1 and 2
    - returns a number between 100 and 200
    - returns a number between 0 and 1 rounded
    - returns a number between 0 and 2 rounded
    - returns a number between 1 and 2 rounded
    - returns a number between 100 and 200 rounded
src/helpers/timestamp.js:
  register:
    - call handlebars registerHelper once
  render:
    - throw no arguments
    - not throw no arguments
    - call mustacher hasoptions once
    - call handlebars createframe once
  returns number:
    - length 13
    - sup a now
    - sup a now + 1000
    - inf a now + 2000
