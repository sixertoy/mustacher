src/mustacher.js:
  hasOptions:
    - returns false if no arguments
    - returns arguments is empty
    - returns false is not a plainobject
    - returns options property 'name' is not defined
    - expect to returns array of arguments
  options:
    - return empty defaults
    - return defaults
    - return concatened context
    - return concatened context + defaults override
src/helpers/conditions.js:
  register:
    - handlebars registerHelper called twice
  render:
    - throws missing arguments
    - not throws missing arguments
    - handlebars createFrame called
  or:
    - return true
    - return false
  and:
    - return true
    - return false
